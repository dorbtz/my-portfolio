# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Next.js
.next/
out/
.vercel/
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files
.env
.env.*

# Local npm config (e.g. Windows --use-system-ca tweaks). Anything machine-
# specific lives only on the dev's box; CI + Vercel pick their own settings.
.npmrc

# Claude Code local working dir + MCP server config
.claude/
.mcp.json

# Test coverage (vitest --coverage output, if ever enabled)
coverage/
.nyc_output/
