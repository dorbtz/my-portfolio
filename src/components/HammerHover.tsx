/**
 * src/components/HammerHover.tsx
 * DEPRECATED — superseded by Cursor.tsx (P1).
 * Kept as a stub so any stale import doesn't break the build.
 * Returns null; all cursor visuals now live in <Cursor />.
 */
export default function HammerHover() {
  return null;
}
