# `legacy/` — Portfolio v1 (Vite + dual-mode)

This folder preserves the v1 React + Vite + dual-fandom (Thor / Luffy) site during
the v2 rebuild. It is kept for reference only and **will be deleted in M9** once
v2 ships to production at https://dorbtz.com.

While the v2 branch (`v2-hightech`) is in flight:
- `main` continues to deploy v1 from the previous structure (pre-rebuild).
- This folder is not built and not deployed. Files here are inert.
- Nothing in `legacy/` imports anything from the new root.

See `../SPEC.md` for the v2 architecture and `../CHANGELOG.md` for the
milestone log.
